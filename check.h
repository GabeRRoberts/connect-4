#ifndef CHECK_H
#define CHECK_H

#include <stdio.h>
#include "config.h"

//Goes through all the check functions and condenses the process to get a single return value.
int win_condition(int g[][NUM_COLS]);

#endif
